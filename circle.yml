machine:
  node:
    version: 6.3.0
  post:
    - npm install -g npm@3
  timezone: Asia/Tokyo
dependencies:
  override:
    - npm install -g firebase-tools eslint eslint-plugin-promise
    - npm —prefix ./functions install ./functions
deployment:
  deploy:
    branch: master
    commands:
      - firebase deploy --project "$FIREBASE_PJ" --token "$FIREBASE_TOKEN"